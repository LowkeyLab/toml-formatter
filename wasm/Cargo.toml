[package]
name = "taplo-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ahash = "=0.8.12"
getrandom = "0.4"
prost = { version = "0.14", default-features = false, features = ["derive"] }
taplo = { version = "0.14", default-features = false }
